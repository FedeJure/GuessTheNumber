#ifndef COMMON_DEBUG_H_
#define COMMON_DEBUG_H_
#include <string>

namespace common
{
    // template<typename T, typename... Args>
    // void log(T message, Args... args);
    // template<typename T>
    // void log(T t);

    // template<>void log(std::string m);
} // namespace common

#endif
